# SteamPipe 集成方案 - 实施设计

## 一、总体架构设计

### 1.1 设计原则

| 原则 | 说明 |
|------|------|
| **最小侵入** | 不修改现有 Electron 打包流程，新增独立的 Steam 打包脚本 |
| **平台抽象** | 保持现有平台抽象层，Steam 版本仅在打包时增加特定配置 |
| **可配置性** | AppID、DepotID 等配置通过环境变量或配置文件注入 |
| **可回滚** | 任何改动均可通过切换分支回滚，不影响主分支功能 |
| **并行构建** | `dist:steam:win` 是独立的并行构建路径，不修改现有 `dist:win`（NSIS 安装器）流程 |

### 1.2 新增目录结构

```
DVStudio/
├── AIPlan/SteamPipe集成方案/           # 本方案文档（已创建）
│   ├── 01_调研分析.md
│   └── 02_实施方案.md
├── scripts/
│   ├── dist-steam-win.mjs             # Windows Steam 打包脚本（新增）
│   └── upload-steam.mjs               # SteamPipe 上传脚本（新增）
├── steam-pipe/                        # SteamPipe 配置目录（新增）
│   ├── app_build.vdf                  # 应用构建配置模板
│   ├── depot_build_win.vdf            # Windows Depot 配置模板
│   ├── depot_build_mac.vdf            # macOS Depot 配置模板
│   ├── depot_build_linux.vdf          # Linux Depot 配置模板
│   ├── steam_appid.txt                # Steam AppID（运行时使用）
│   └── env.example                    # 环境变量示例
└── .github/workflows/
    └── steam-build.yml                # GitHub Actions Steam 构建工作流（新增）
```

### 1.3 新增文件清单

| 文件 | 类型 | 说明 |
|------|------|------|
| `scripts/dist-steam-win.mjs` | 脚本 | Windows Steam 版本打包脚本 |
| `scripts/upload-steam.mjs` | 脚本 | 调用 steamcmd 上传构建到 SteamPipe |
| `steam-pipe/app_build.vdf` | 配置 | 应用构建配置模板（含 AppID 占位符） |
| `steam-pipe/depot_build_win.vdf` | 配置 | Windows Depot 配置模板 |
| `steam-pipe/depot_build_mac.vdf` | 配置 | macOS Depot 配置模板 |
| `steam-pipe/depot_build_linux.vdf` | 配置 | Linux Depot 配置模板 |
| `steam-pipe/steam_appid.txt` | 配置 | Steam AppID 文件 |
| `steam-pipe/env.example` | 配置 | 环境变量示例文件 |
| `.github/workflows/steam-build.yml` | CI/CD | GitHub Actions 自动化构建上传 |

### 1.4 .gitignore 规则

`steam-pipe/` 目录包含大量生成的构建产物，**必须加入 `.gitignore`，不能上传到 GitHub**：

```gitignore
# SteamPipe build artifacts (never commit generated content)
steam-pipe/*
!steam-pipe/app_build.vdf
!steam-pipe/depot_build_win.vdf
!steam-pipe/depot_build_mac.vdf
!steam-pipe/depot_build_linux.vdf
!steam-pipe/env.example
```

**规则说明**：
- `steam-pipe/*`：忽略 steam-pipe 目录下的所有内容（但不忽略目录本身，允许例外规则生效）
- `!steam-pipe/app_build.vdf`：但保留应用构建配置模板
- `!steam-pipe/depot_build_*.vdf`：但保留各平台 Depot 配置模板
- `!steam-pipe/env.example`：但保留环境变量示例文件

**关键注意**：使用 `steam-pipe/*` 而非 `steam-pipe/`，因为 git 不会扫描被完全忽略的目录，导致例外规则失效。

**注意**：构建产物（`content/`、`output/`、`build/`）和生成的 VDF 文件（`*_generated.vdf`）会被自动忽略，不应提交到版本控制。

**AIPlan 目录说明**：本方案文档所在的 `AIPlan/` 目录已被项目根 `.gitignore` 忽略，如需提交方案文档，需使用 `git add -f` 强制添加：
```bash
git add -f AIPlan/SteamPipe集成方案/
```

---

## 二、详细设计

### 2.1 打包脚本设计

#### 2.1.1 `scripts/dist-steam-win.mjs`

**功能**：生成符合 Steam 上架要求的 Windows 构建产物

**流程**：
```
1. 检查环境变量（STEAM_APP_ID）
2. 构建 DwebSteamJS 原生模块（针对 Electron 33.x）
3. 部署原生模块到 electron/platform/native/win32/
4. 创建/更新 steam_appid.txt
5. 执行 vite build（前端构建）
6. 执行 electron-builder（生成 portable 版本，无安装器）
7. 将构建产物整理到 steam-pipe/content/ 目录
```

**关键配置差异**：

| 配置项 | 普通版本 | Steam 版本 |
|--------|----------|------------|
| `target` | `nsis`（安装器） | `dir`（便携式目录） |
| `asar` | `true` | `true` |
| `asarUnpack` | 无 | `electron/platform/native/**`（原生模块必须从 asar 中解压） |
| `extraResources` | Python Bridge 脚本 | 增加 Steam 原生模块 |
| `output` | `release-<timestamp>` | `steam-pipe/build/win64-unpacked` |

**electron-builder --dir 输出结构**：
```
steam-pipe/build/win64-unpacked/
├── DVStudio.exe
├── resources/
│   ├── app.asar
│   ├── app.asar.unpacked/
│   │   └── electron/platform/native/win32/
│   │       ├── dweb_steamjs.node
│   │       └── steam_api64.dll
│   ├── python-bridge-scripts/
│   ├── static/
│   └── runtime/
└── [其他系统文件]
```

**后处理整理结构（SteamPipe 期望）**：
```
steam-pipe/content/win64/
├── DVStudio.exe
├── resources/
│   ├── app.asar
│   ├── app.asar.unpacked/
│   │   └── electron/platform/native/win32/
│   │       ├── dweb_steamjs.node
│   │       └── steam_api64.dll
│   ├── python-bridge-scripts/
│   ├── static/
│   └── runtime/
└── steam_appid.txt（仅开发测试用，不上传到 depot）
```

**核心代码逻辑**：
```javascript
// 1. 构建原生模块
await run('npm', ['run', 'build:electron'], { cwd: 'DwebSteamJS' })

// 2. 部署原生模块
await run('node', ['scripts/setup-steam-native.mjs'])

// 3. 前端构建
await run('npx', ['vite', 'build'])

// 4. Electron 打包（便携式目录，使用 asarUnpack 解压原生模块）
//    注意：仅使用 asarUnpack，不使用 extraResources 复制原生模块，避免冗余
await run('electron-builder', [
  '--dir',
  '-w',
  '--publish', 'never',
  '--config.directories.output', 'steam-pipe/build/win64-unpacked',
  '--config.asarUnpack', 'electron/platform/native/**',
  '--config.extraResources', JSON.stringify([
    { from: 'electron/backend/python-bridge/scripts', to: 'python-bridge-scripts' },
    { from: 'electron/static', to: 'static', filter: ['**/*', '!runtime/**'] },
    { from: 'electron/static/runtime', to: 'runtime' }
  ])
])

// 5. 后处理：整理构建产物到 steam-pipe/content/win64
const unpackedDir = 'steam-pipe/build/win64-unpacked'
const contentDir = 'steam-pipe/content/win64'
fs.rmSync(contentDir, { recursive: true, force: true })
fs.cpSync(unpackedDir, contentDir, { recursive: true })

// 6. 开发测试用：复制 steam_appid.txt 到构建目录（不上传到 SteamPipe）
//    Steam 启动时会自动提供 steam_appid.txt，不需要包含在 depot 中
fs.writeFileSync(path.join(contentDir, 'steam_appid.txt'), process.env.STEAM_APP_ID || '480')
```

#### 2.1.2 `scripts/upload-steam.mjs`

**功能**：调用 steamcmd 将构建产物上传到 SteamPipe

**流程**：
```
1. 读取环境变量（STEAM_USERNAME, STEAM_PASSWORD, STEAM_APP_ID）
2. 验证构建产物是否存在
3. 生成实际的 .vdf 配置文件（替换占位符）
4. 调用 steamcmd 执行构建上传
5. 解析输出，检查是否成功
```

**核心代码逻辑**：
```javascript
// 1. 环境变量验证
const requiredEnv = ['STEAM_APP_ID']
requiredEnv.forEach(key => {
  if (!process.env[key]) throw new Error(`Missing ${key}`)
})

// 2. 生成配置文件（替换占位符）
const vdfTemplate = fs.readFileSync('steam-pipe/app_build.vdf', 'utf8')
const vdfContent = vdfTemplate
  .replace('{{APP_ID}}', process.env.STEAM_APP_ID)
  .replace('{{BUILD_OUTPUT}}', 'steam-pipe/output')
  .replace('{{VERSION}}', pkg.version)

fs.writeFileSync('steam-pipe/app_build_generated.vdf', vdfContent)

// 3. 调用 steamcmd（支持 Steam Guard 2FA）
//    steamcmd 登录格式：+login <username> <password> <steam_guard_code>
//    如果账户启用了 Steam Guard，需要提供 steam_guard_code
//    首次登录后会生成 Sentry 文件，后续登录不需要验证码（除非安全设置变更）
const steamcmdPath = 'SteamworksSDK/sdk/sdk/tools/ContentBuilder/builder/steamcmd.exe'
const loginArgs = ['+login']
if (process.env.STEAM_USERNAME) loginArgs.push(process.env.STEAM_USERNAME)
if (process.env.STEAM_PASSWORD) loginArgs.push(process.env.STEAM_PASSWORD)
if (process.env.STEAM_GUARD_CODE) loginArgs.push(process.env.STEAM_GUARD_CODE)

await run(steamcmdPath, [
  ...loginArgs,
  '+run_app_build', 'steam-pipe/app_build_generated.vdf',
  '+quit'
])
```

**Steam Guard 2FA 处理方案**：

| 场景 | 方案 | 说明 |
|------|------|------|
| 首次登录 | 手动输入验证码 | 运行脚本时设置 STEAM_GUARD_CODE 环境变量 |
| 后续登录 | 使用 Sentry 文件 | steamcmd 首次成功登录后会在用户目录生成 Sentry 文件 |
| CI/CD 环境 | 使用专用构建账户 + 手机验证码 | 或使用 steamcmd 的 `-remember_password` 参数 |
| 无法自动化 | 手动上传 | 在 Steamworks 后台手动上传构建 |

**注意**：
- 启用 Steam Guard 的账户在安全设置变更后（如更换邮箱、手机号），需要等待 3 天才能设置构建上线
- 建议创建专用的构建账户，仅授予"Edit App Metadata"和"Publish App Changes To Steam"权限
- steamcmd 登录失败时会提示验证码要求，脚本应捕获并提示用户提供

### 2.2 SteamPipe 配置模板设计

#### 2.2.1 `steam-pipe/app_build.vdf`

```vdf
"appbuild"
{
	"appid" "{{APP_ID}}"
	"desc" "DVStudio {{VERSION}} (Steam Release)"
	"buildoutput" "{{BUILD_OUTPUT}}"
	"contentroot" "steam-pipe/content"
	"setlive" "beta"
	"preview" "0"
	"local" "0"
	"depots"
	{
		"{{DEPOT_ID_WIN}}" "depot_build_win.vdf"
		"{{DEPOT_ID_MAC}}" "depot_build_mac.vdf"
		"{{DEPOT_ID_LINUX}}" "depot_build_linux.vdf"
	}
}
```

**占位符说明**：
- `{{APP_ID}}`：Steam 应用 ID
- `{{VERSION}}`：应用版本号（从 package.json 读取）
- `{{BUILD_OUTPUT}}`：构建输出目录
- `{{DEPOT_ID_WIN}}`：Windows Depot ID
- `{{DEPOT_ID_MAC}}`：macOS Depot ID
- `{{DEPOT_ID_LINUX}}`：Linux Depot ID

#### 2.2.2 `steam-pipe/depot_build_win.vdf`

```vdf
"depotbuild"
{
	"depotid" "{{DEPOT_ID_WIN}}"
	"contentroot" "steam-pipe/content/win64"
	"depotfrommanifest" "0"
	"filemapping"
	{
		"local" "."
		"depot" "."
	}
	"ignore"
	{
		"*.pdb"
		"*.bak"
		"*.log"
		"node_modules/"
		"steam_appid.txt"
	}
}
```

**注意**：`steam_appid.txt` 被排除在 depot 之外，因为：
- Steam 客户端启动应用时会自动在工作目录创建 `steam_appid.txt`
- 包含在 depot 中可能会覆盖 Steam 提供的文件，导致冲突
- 开发测试时需要手动创建 `steam_appid.txt`（已在打包脚本中处理，仅用于本地测试）

#### 2.2.3 `steam-pipe/env.example`

```bash
# Steam 构建环境变量
STEAM_APP_ID=480
STEAM_DEPOT_ID_WIN=1001
STEAM_DEPOT_ID_MAC=1002
STEAM_DEPOT_ID_LINUX=1003
STEAM_USERNAME=your_build_account
STEAM_PASSWORD=your_build_password
STEAM_BRANCH=beta
```

### 2.3 package.json 脚本配置

**新增脚本**：
```json
{
  "scripts": {
    "dist:steam:win": "node scripts/dist-steam-win.mjs",
    "upload:steam": "node scripts/upload-steam.mjs",
    "build:steam": "npm run dist:steam:win && npm run upload:steam"
  }
}
```

**完整脚本列表**：

| 脚本 | 说明 |
|------|------|
| `dist:steam:win` | 构建 Windows Steam 版本 |
| `upload:steam` | 上传到 SteamPipe |
| `build:steam` | 构建并上传（完整流程） |

### 2.4 原生模块打包配置

**问题分析**：

当前 `package.json#build.extraResources` 不包含 Steam 原生模块，导致打包后无法加载 Steamworks SDK。

**解决方案**：

在 Steam 打包脚本中动态扩展 `extraResources`：

```javascript
const extraResources = [
  {
    from: 'electron/backend/python-bridge/scripts',
    to: 'python-bridge-scripts'
  },
  {
    from: 'electron/static',
    to: 'static',
    filter: ['**/*', '!runtime/**']
  },
  {
    from: 'electron/static/runtime',
    to: 'runtime'
  },
  {
    from: 'electron/platform/native/win32',
    to: 'platform/native/win32'
  }
]
```

**运行时路径解析**：

修改 `electron/platform/providers/steam.mjs` 中的路径解析逻辑：

```javascript
function getNativeWin32Dir() {
  if (app.isPackaged) {
    const unpackedPath = path.join(process.resourcesPath, 'app.asar.unpacked', 'electron', 'platform', 'native', 'win32')
    if (fs.existsSync(unpackedPath)) {
      return unpackedPath
    }
    console.warn('[platform:steam] Native module directory not found at:', unpackedPath)
    return process.resourcesPath
  }
  return path.join(__dirname, '..', 'native', 'win32')
}
```

**路径说明**：
- 仅使用 `asarUnpack` 解压路径：`resources/app.asar.unpacked/electron/platform/native/win32`
- 不使用 `extraResources` 复制原生模块，避免文件冗余
- 开发环境（非打包状态）使用 `electron/platform/native/win32/` 路径

**测试建议**：在实施路径解析逻辑前，先执行一次测试打包：
```bash
npm run build
npx electron-builder --dir -w --config.asarUnpack="electron/platform/native/**"
```
然后检查输出目录中的实际路径结构，确保代码中的路径与实际输出一致。

### 2.5 GitHub Actions 工作流设计

#### `.github/workflows/steam-build.yml`

**触发条件**：
- 手动触发（workflow_dispatch）
- 推送标签（v*）

**环境变量**：
- `STEAM_USERNAME`：GitHub Secrets
- `STEAM_PASSWORD`：GitHub Secrets
- `STEAM_APP_ID`：GitHub Secrets
- `STEAM_DEPOT_ID_WIN`：GitHub Secrets

**步骤**：
```yaml
1. Checkout 代码
2. 设置 Node.js 环境
3. 安装依赖
4. 构建 DwebSteamJS 原生模块
5. 运行 dist:steam:win 打包
6. 保存构建产物为 Artifact（供手动上传）
7. 尝试自动上传（可选，需要 Steam Guard 2FA 已解决）
8. 保存构建日志
```

**Steam Guard 2FA 限制**：
- GitHub Actions 环境中无法自动处理 Steam Guard 验证码
- 解决方案：
  1. 在首次运行时手动提供验证码，生成 Sentry 文件后缓存到 GitHub Cache
  2. 使用专用构建账户，在本地首次登录生成 Sentry 文件后上传到 Secrets
  3. 放弃自动上传，仅自动构建，手动在 Steamworks 后台上传
- 建议初期采用方案 3（手动上传），待 2FA 问题解决后再启用自动上传

---

## 三、实施步骤

### 3.1 第一阶段：基础配置（P0）

| 步骤 | 任务 | 负责人 | 预估时间 |
|------|------|--------|----------|
| 1.1 | 创建 `steam-pipe/` 目录及配置模板 | AI | 0.5 天 |
| 1.2 | 创建 `scripts/dist-steam-win.mjs` | AI | 1 天 |
| 1.3 | 更新 `package.json` 添加 Steam 脚本 | AI | 0.5 天 |
| 1.4 | 测试 Windows Steam 打包流程 | 开发 | 1 天 |

### 3.2 第二阶段：上传集成（P1）

| 步骤 | 任务 | 负责人 | 预估时间 |
|------|------|--------|----------|
| 2.1 | 创建 `scripts/upload-steam.mjs` | AI | 1 天 |
| 2.2 | 修改 `electron/platform/providers/steam.mjs` 路径解析 | AI | 0.5 天 |
| 2.3 | 测试上传流程（使用测试 AppID） | 开发 | 1 天 |

### 3.3 第三阶段：多平台支持（P2）

| 步骤 | 任务 | 负责人 | 预估时间 |
|------|------|--------|----------|
| 3.1 | 构建 macOS 原生模块 | 开发 | 1 天 |
| 3.2 | 创建 macOS Steam 打包脚本 | AI | 1 天 |
| 3.3 | 构建 Linux 原生模块 | 开发 | 1 天 |
| 3.4 | 创建 Linux Steam 打包脚本 | AI | 1 天 |

### 3.4 第四阶段：CI/CD 集成（P2）

| 步骤 | 任务 | 负责人 | 预估时间 |
|------|------|--------|----------|
| 4.1 | 创建 GitHub Actions 工作流 | AI | 1 天 |
| 4.2 | 配置 GitHub Secrets | 开发 | 0.5 天 |
| 4.3 | 测试自动化构建上传 | 开发 | 1 天 |

---

## 四、风险评估

### 4.1 技术风险

| 风险 | 等级 | 应对措施 |
|------|------|----------|
| 原生模块 ABI 不兼容 | 高 | 使用 Electron 版本对应的 node-gyp 构建 |
| asar 打包导致原生模块无法加载 | 高 | 使用 `asarUnpack` 将 `electron/platform/native/**` 从 asar 中解压 |
| steamcmd 认证失败（Steam Guard 2FA） | 高 | 使用专用构建账户，首次登录提供验证码生成 Sentry 文件；CI/CD 初期采用手动上传 |
| 构建产物过大 | 中 | 清理不必要文件，使用 asar 压缩 |
| 多平台差异 | 中 | 分别构建，使用平台特定配置 |
| 运行时路径解析错误 | 中 | 在打包脚本中同时使用 asarUnpack 和 extraResources，运行时按优先级查找；先做测试打包确认路径 |

### 4.2 业务风险

| 风险 | 等级 | 应对措施 |
|------|------|----------|
| Steamworks 后台配置错误 | 高 | 仔细核对 AppID、DepotID、Launch Options |
| 版本冲突 | 中 | 使用分支管理（beta 分支测试） |
| 审核失败 | 中 | 提前准备测试，确保符合 Steam 上架要求 |

---

## 五、验证方案

### 5.1 本地验证

**Windows Steam 版本验证步骤**：

1. 设置环境变量：`set STEAM_APP_ID=480`
2. 运行打包脚本：`npm run dist:steam:win`
3. 检查输出目录 `steam-pipe/content/win64/`：
   - ✅ `DVStudio.exe` 存在
   - ✅ `resources/app.asar` 存在（应用代码归档）
   - ✅ `resources/app.asar.unpacked/electron/platform/native/win32/dweb_steamjs.node` 存在（asarUnpack 解压的原生模块）
   - ✅ `resources/app.asar.unpacked/electron/platform/native/win32/steam_api64.dll` 存在
   - ❌ `resources/platform/native/` 不存在（不应通过 extraResources 复制，避免冗余）
   - ⚠️ `steam_appid.txt` 存在（仅开发测试用，不上传到 SteamPipe）
4. 启动 Steam 客户端，运行 SpaceWar（AppID 480）测试环境
5. 运行构建产物，检查 Steam 集成是否正常

### 5.2 上传验证

**上传流程验证步骤**：

1. 设置环境变量：`STEAM_USERNAME`, `STEAM_PASSWORD`, `STEAM_APP_ID`
2. 运行上传脚本：`npm run upload:steam`
3. 检查 `steam-pipe/output/` 日志：
   - ✅ 无错误信息
   - ✅ 显示"Success!"
4. 登录 Steamworks 后台，检查构建是否出现在"Builds"页面
5. 设置为测试分支，测试下载和安装

### 5.3 Steam 商店页全流程测试

#### 5.3.1 测试环境准备

**前提条件**：
- ✅ 已在 Steamworks 后台创建正式 AppID（非 SpaceWar 480）
- ✅ 已配置至少一个 Depot（Windows）
- ✅ 已配置 Launch Options
- ✅ 已创建测试分支（如 `beta`）
- ✅ 已设置构建账户权限（Edit App Metadata + Publish App Changes To Steam）
- ✅ 测试账户已添加到 Steamworks 后台的测试账户列表

**测试账户添加步骤**：
1. 登录 Steamworks 后台 → 应用管理 → 测试账户
2. 添加测试账户的 SteamID
3. 选择测试分支权限（如 `beta`）
4. 保存更改

#### 5.3.2 构建上传流程测试

**步骤**：

1. **构建 Steam 版本**
   ```bash
   set STEAM_APP_ID=<你的正式AppID>
   npm run dist:steam:win
   ```
   验证：
   - ✅ 构建成功完成
   - ✅ `steam-pipe/content/win64/` 目录结构正确

2. **生成 VDF 配置文件**
   ```bash
   # 确保环境变量已设置
   set STEAM_APP_ID=<你的正式AppID>
   set STEAM_DEPOT_ID_WIN=<你的DepotID>
   ```
   验证：
   - ✅ `steam-pipe/app_build_generated.vdf` 生成成功
   - ✅ 占位符已正确替换

3. **上传到 SteamPipe**
   ```bash
   # 首次登录需要 Steam Guard 验证码
   set STEAM_USERNAME=<构建账户用户名>
   set STEAM_PASSWORD=<构建账户密码>
   set STEAM_GUARD_CODE=<验证码>
   npm run upload:steam
   ```
   验证：
   - ✅ steamcmd 登录成功
   - ✅ 构建上传成功
   - ✅ 输出日志显示"Success!"

4. **在 Steamworks 后台验证构建**
   - 登录 Steamworks 后台 → 应用管理 → 构建
   - ✅ 新构建出现在构建列表中
   - ✅ 构建描述包含版本号
   - ✅ 显示构建大小和文件数量
   - ✅ 显示"设置上线"按钮

#### 5.3.3 测试分支设置测试

**步骤**：

1. **设置构建到测试分支**
   - 在构建页面，找到刚上传的构建
   - 点击"设置上线"
   - 选择测试分支（如 `beta`）
   - 点击"设置上线"确认

2. **验证分支设置**
   - ✅ 构建状态变为"已上线到 beta"
   - ✅ 显示"上线时间"和"上次更新时间"
   - ✅ 可以随时回滚到之前的构建

#### 5.3.4 Steam 客户端下载安装测试

**步骤**：

1. **登录测试账户**
   - 退出当前 Steam 账户
   - 登录测试账户（已添加到测试账户列表）

2. **找到测试应用**
   - 在 Steam 客户端中搜索应用名称
   - ✅ 应用出现在搜索结果中（即使尚未上架）
   - ✅ 显示"安装"按钮

3. **开始下载**
   - 点击"安装"
   - ✅ 下载开始
   - ✅ 显示下载进度
   - ✅ 显示预计剩余时间
   - ✅ 下载完成后自动开始安装

4. **验证安装**
   - ✅ 安装进度条完成
   - ✅ 显示"开始"按钮
   - ✅ 安装目录正确（默认 `Steam\steamapps\common\<AppName>`）

#### 5.3.5 启动与功能测试

**步骤**：

1. **启动应用**
   - 点击"开始"按钮
   - ✅ 应用启动
   - ✅ 显示 Steam Overlay（按 Shift+Tab 测试）

2. **Steam 集成功能测试**
   - ✅ 用户信息正确显示（SteamID、昵称、头像）
   - ✅ 好友列表可访问
   - ✅ 好友在线状态正确
   - ✅ DLC 检测正常（如配置了 DLC）
   - ✅ 在 Steam 好友列表中显示"正在玩 DVStudio"

3. **核心功能测试**
   - ✅ 项目列表页面正常显示
   - ✅ 视频编辑器正常工作
   - ✅ AI 工作流蓝图正常工作
   - ✅ 导出功能正常
   - ✅ 本地数据库正常（DVSResource 目录正确创建）

4. **关闭应用**
   - ✅ 应用正常退出
   - ✅ Steam 好友列表显示状态更新
   - ✅ 无崩溃或错误日志

#### 5.3.6 更新流程测试

**步骤**：

1. **发布新版本**
   - 修改应用代码（如版本号）
   - 重新构建并上传：`npm run dist:steam:win && npm run upload:steam`
   - 在 Steamworks 后台设置新构建到测试分支

2. **验证更新提示**
   - 在 Steam 客户端中，测试账户会收到更新提示
   - ✅ 显示"更新可用"
   - ✅ 显示更新大小

3. **下载更新**
   - 点击"更新"
   - ✅ 更新下载开始
   - ✅ 显示更新进度
   - ✅ 更新完成后自动安装

4. **验证更新**
   - ✅ 应用版本号已更新
   - ✅ 新功能正常工作
   - ✅ 旧数据（项目、设置）保留

#### 5.3.7 离线模式测试

**步骤**：

1. **切换到离线模式**
   - 退出 Steam
   - 重新启动 Steam，选择"离线模式"

2. **启动应用**
   - ✅ 应用正常启动（无需联网）
   - ✅ 核心功能正常
   - ✅ Steam 集成功能降级为离线模式（如好友列表不可用）

3. **切换回在线模式**
   - 退出应用
   - Steam 切换回在线模式
   - 重新启动应用
   - ✅ Steam 集成功能恢复正常

#### 5.3.8 卸载测试

**步骤**：

1. **卸载应用**
   - 在 Steam 客户端中，右键应用 → 管理 → 卸载
   - ✅ 卸载确认对话框显示
   - ✅ 卸载进度条完成

2. **验证卸载**
   - ✅ 安装目录已删除
   - ✅ Steam 库中显示"安装"按钮（而非"开始"）
   - ✅ 用户数据目录（DVSResource）根据配置决定是否保留

#### 5.3.9 测试检查清单

| 测试项 | 预期结果 | 实际结果 |
|--------|----------|----------|
| 构建上传成功 | ✅ | |
| 构建出现在 Steamworks 后台 | ✅ | |
| 测试分支设置成功 | ✅ | |
| Steam 客户端搜索到应用 | ✅ | |
| 下载正常 | ✅ | |
| 安装正常 | ✅ | |
| 启动正常 | ✅ | |
| Steam Overlay 可用 | ✅ | |
| 用户信息正确 | ✅ | |
| 好友列表正常 | ✅ | |
| 核心功能正常 | ✅ | |
| 更新流程正常 | ✅ | |
| 离线模式正常 | ✅ | |
| 卸载正常 | ✅ | |
| 无崩溃 | ✅ | |

#### 5.3.10 常见问题排查

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 应用无法启动 | Steam 未运行或未通过 Steam 启动 | 确保通过 Steam 客户端启动 |
| Steam 集成功能不可用 | 原生模块路径错误 | 检查 `app.asar.unpacked` 路径 |
| 下载失败 | Depot 配置错误 | 检查 Depot ID 和内容根目录 |
| 更新过大 | 文件结构不符合 SteamPipe 最佳实践 | 优化文件打包策略 |
| 审核失败 | 缺少必要信息或功能 | 查看 Steamworks 审核反馈 |

#### 5.3.11 测试环境清理

**测试完成后清理步骤**：

1. **取消测试分支上线**
   - 在 Steamworks 后台 → 构建 → 点击"取消上线"
   - 选择测试分支

2. **清理测试构建**
   - 删除不需要的测试构建版本

3. **清理本地构建产物**
   ```bash
   rm -rf steam-pipe/build/
   rm -rf steam-pipe/content/
   rm -rf steam-pipe/output/
   ```

4. **恢复环境变量**
   - 删除临时设置的环境变量

---

## 六、回滚方案

### 6.1 代码回滚

```bash
# 切换回主分支
git checkout main

# 删除 Steam 集成分支（如需）
git branch -D feature/steam-pipe-integration
```

### 6.2 构建回滚

在 Steamworks 后台：
1. 进入"Builds"页面
2. 找到之前的稳定构建
3. 点击"Set Live"设置为公开版本

---

## 七、注意事项

### 7.1 AppID 管理

- 开发阶段使用 SpaceWar AppID（480）进行测试
- 正式上架前需要申请正式 AppID
- `steam_appid.txt` 必须与可执行文件同目录

### 7.2 原生模块构建

- 必须使用与 Electron 版本匹配的 node-gyp 配置
- 命令：`npm run build:electron`（在 DwebSteamJS 目录）
- 构建产物必须复制到 `electron/platform/native/win32/`

### 7.3 Steam 启动要求

- Steam 版本必须通过 Steam 客户端启动
- `RestartAppIfNecessary` 在非 Steam 启动时会返回 `true`
- 开发环境通过环境变量 `ELECTRON_DEV` 跳过此检查

### 7.4 内容大小限制

- 单个 Depot 建议不超过 2GB
- 大文件会影响更新速度
- 建议将 Python 运行时等大文件放在单独的 Depot